import { MovieValidationPipe } from './movie-validation.pipe';

describe('MovieValidationPipe', () => {
  it('should be defined', () => {
    expect(new MovieValidationPipe()).toBeDefined();
  });
});
